
spring:
  batch:
    job:
      #设置为 false -需要jobLaucher.run执行
      enabled: false
      #设置为always
    initialize-schema: always
  #    table-prefix: my-batch
  datasource:
    url: jdbc:mysql://localhost:3306/batch-test?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8&zeroDateTimeBehavior=convertToNull
    username: root
    password: root
  driver-class-name: com.mysql.cj.jdbc.Driver



server:
  port: 8665
